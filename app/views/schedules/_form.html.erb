<%= simple_form_for(@schedule) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :event %>
    <%= f.association :batch %>
    <%= f.input :datestart %>
    <%= f.input :dateend %>
    <%= f.input :observation %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
