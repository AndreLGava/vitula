<%= simple_form_for(@batchanimal) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :animal %>
    <%= f.association :batch %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
