<%= simple_form_for(@bin) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :capacity  ,as: :normal, before: "M³",:input_html => {value: number_with_precision(f.object.capacity, precision: 2) }%>
    <%= f.association :property, collection: Property.where(user_id: current_user.id) , allow_blank: false %>
    <%= f.input :kind, collection: Bin.kinds.keys, as: :select, allow_blank: false %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
